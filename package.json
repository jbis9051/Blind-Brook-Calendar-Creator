{
  "name": "@bb-schedler/bb-scheduler",
  "version": "2.1.0",
  "private": true,
  "description": "A website and module to generate iCal files and HTML tables based on Blind Brook's rotating block schedule and letter day calendar.",
  "scripts": {
    "setup": "lerna bootstrap",
    "start": "lerna run --parallel start"
  },
  "dependencies": {
    "lerna": "^3.22.1"
  }
}
